{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the CC Desktop Switch desktop window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
