{
  "name": "cc-desktop-switch",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm build && vite preview --host 127.0.0.1",
    "build": "cargo run -p xtask -- frontend build",
    "check:static": "cargo run -p xtask -- frontend check-contracts",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.0",
    "vite": "^7.2.0"
  },
  "packageManager": "pnpm@10.29.2"
}
